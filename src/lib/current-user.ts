// Auth is not wired up yet — dashboard data is scoped to the seeded demo user.
// Extracted here so there is a single place to change when real (session-based)
// user scoping lands.
export const DEMO_USER_EMAIL = "demo@devstash.io";
